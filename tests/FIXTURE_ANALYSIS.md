# DQAR UC1 — Fixture Pipeline Analysis

Generated by `tests/generate_fixtures.py`.
HAPI Validator JAR: found.

---

## Summary Table

```
Fixture                                   1a-ii     1b        1c-i      1c-ii     Notes
--------------------------------------------------------------------------------------------------------------
fixture-01-baseline-clean                 PASS      PASS      PASS      PASS      Baseline: all stages should pass.
fixture-02-1b-utf8-encoding               PASS      FAIL      NOT_RUN   NOT_RUN   Stage 1b should fail utf8_decodable for Patient.
fixture-03-1b-invalid-json                PASS      FAIL      NOT_RUN   NOT_RUN   Stage 1b should fail all_lines_valid_json for Condition.
fixture-04-1b-missing-resourcetype        FAIL      FAIL      NOT_RUN   NOT_RUN   Stage 1b should fail resource_type_present for Encounter.
fixture-05-1b-filename-mismatch           PASS      FAIL      NOT_RUN   NOT_RUN   1b should fail filename_matches_type. 1a2 may also flag mixe
fixture-06-1b-empty-files                 PASS      EMPTY     NOT_RUN   NOT_RUN   Stage 1b marks empty files; not a hard FAIL. 1a2 may still p
fixture-07-1b-mixed-types                 FAIL      FAIL      NOT_RUN   NOT_RUN   1b should fail filename_matches_type. 1a2 should fail single
fixture-08-1a2-no-patient                 FAIL      PASS      NOT_RUN   NOT_RUN   1a2 should fail patient_file_present.
fixture-09-1a2-missing-core-types         FAIL      PASS      NOT_RUN   NOT_RUN   1a2 should fail hedis_core_types_present.
fixture-10-1c-base-r4-errors              PASS      PASS      PASS      PASS      1c-i should catch missing required base R4 fields.
fixture-11-1c-uscore-errors               PASS      PASS      PASS      PASS      1c-ii should catch US Core profile violations.
fixture-12-1c-combined-violations         PASS      PASS      PASS      PASS      Both 1c-i and 1c-ii should fail.
fixture-13-1b-all-structural-errors       FAIL      FAIL      NOT_RUN   NOT_RUN   Multiple 1b failure modes in one extract.
```

Status key: PASS / FAIL / EMPTY / SKIPPED / NOT_RUN / ERROR

---

## Per-Fixture Analysis

### fixture-01-baseline-clean

**Injected errors:** No mutations — all source files copied verbatim.

**Notes:** Baseline: all stages should pass.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **PASS** | — |
| 1c-i | **PASS** | 0 errors, 0 warnings |
| 1c-ii | **PASS** | 0 errors, 0 warnings |

---

### fixture-02-1b-utf8-encoding

**Injected errors:** Patient.ndjson: 3 lines contain 0x80 byte (invalid UTF-8).

**Notes:** Stage 1b should fail utf8_decodable for Patient.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **FAIL** | 1 file(s) failed, 0 empty; Patient.ndjson: utf8_decodable, no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

**Stage 1b file-level failures:**

- `Patient.ndjson` — **FAIL**
  - Failed checks: utf8_decodable, no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type
  - Issue counts: {'utf8_errors': 1, 'json_parse_errors': 0, 'missing_resource_type': 0, 'resource_type_mismatches': 0}

---

### fixture-03-1b-invalid-json

**Injected errors:** Condition.ndjson: first 5 lines truncated at position 40.

**Notes:** Stage 1b should fail all_lines_valid_json for Condition.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **FAIL** | 1 file(s) failed, 0 empty; Condition.ndjson: all_lines_valid_json |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

**Stage 1b file-level failures:**

- `Condition.ndjson` — **FAIL**
  - Failed checks: all_lines_valid_json
  - Issue counts: {'utf8_errors': 0, 'json_parse_errors': 5, 'missing_resource_type': 0, 'resource_type_mismatches': 0}

---

### fixture-04-1b-missing-resourcetype

**Injected errors:** Encounter.ndjson: resourceType removed from first 5 records.

**Notes:** Stage 1b should fail resource_type_present for Encounter.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **FAIL** | failed checks: single_type_per_file |
| 1b | **FAIL** | 1 file(s) failed, 0 empty; Encounter.ndjson: resource_type_present |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

**Stage 1b file-level failures:**

- `Encounter.ndjson` — **FAIL**
  - Failed checks: resource_type_present
  - Issue counts: {'utf8_errors': 0, 'json_parse_errors': 0, 'missing_resource_type': 5, 'resource_type_mismatches': 0}

---

### fixture-05-1b-filename-mismatch

**Injected errors:** Procedure.ndjson: replaced with Observation records (resourceType=Observation).

**Notes:** 1b should fail filename_matches_type. 1a2 may also flag mixed types.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **FAIL** | 1 file(s) failed, 0 empty; Procedure.ndjson: filename_matches_type |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

**Stage 1b file-level failures:**

- `Procedure.ndjson` — **FAIL**
  - Failed checks: filename_matches_type
  - Issue counts: {'utf8_errors': 0, 'json_parse_errors': 0, 'missing_resource_type': 0, 'resource_type_mismatches': 9}

---

### fixture-06-1b-empty-files

**Injected errors:** Coverage.ndjson: 0 bytes. DiagnosticReport.ndjson: 0 bytes added.

**Notes:** Stage 1b marks empty files; not a hard FAIL. 1a2 may still pass.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **EMPTY** | 0 file(s) failed, 2 empty; Coverage.ndjson: no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type; DiagnosticReport.ndjson: no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

**Stage 1b file-level failures:**

- `Coverage.ndjson` — **EMPTY**
  - Failed checks: no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type
- `DiagnosticReport.ndjson` — **EMPTY**
  - Failed checks: no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type

---

### fixture-07-1b-mixed-types

**Injected errors:** Condition.ndjson: 3 Observation records appended (two types in one file).

**Notes:** 1b should fail filename_matches_type. 1a2 should fail single_type_per_file.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **FAIL** | failed checks: single_type_per_file |
| 1b | **FAIL** | 1 file(s) failed, 0 empty; Condition.ndjson: filename_matches_type |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

**Stage 1b file-level failures:**

- `Condition.ndjson` — **FAIL**
  - Failed checks: filename_matches_type
  - Issue counts: {'utf8_errors': 0, 'json_parse_errors': 0, 'missing_resource_type': 0, 'resource_type_mismatches': 3}

---

### fixture-08-1a2-no-patient

**Injected errors:** Patient.ndjson omitted from extract.

**Notes:** 1a2 should fail patient_file_present.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **FAIL** | failed checks: patient_file_present |
| 1b | **PASS** | — |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

---

### fixture-09-1a2-missing-core-types

**Injected errors:** Condition.ndjson and Observation.ndjson omitted.

**Notes:** 1a2 should fail hedis_core_types_present.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **FAIL** | failed checks: hedis_core_types_present |
| 1b | **PASS** | — |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

---

### fixture-10-1c-base-r4-errors

**Injected errors:** Observation.ndjson: status stripped from all records. MedicationRequest.ndjson: status+intent stripped from all records. Encounter.ndjson: status+class stripped from first 10 records.

**Notes:** 1c-i should catch missing required base R4 fields.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **PASS** | — |
| 1c-i | **PASS** | 0 errors, 0 warnings |
| 1c-ii | **PASS** | 0 errors, 0 warnings |

---

### fixture-11-1c-uscore-errors

**Injected errors:** Patient.ndjson: identifier removed from all records. Condition.ndjson: category removed from all records. Observation.ndjson: category removed from all records. Encounter.ndjson: type removed from all records.

**Notes:** 1c-ii should catch US Core profile violations.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **PASS** | — |
| 1c-i | **PASS** | 0 errors, 0 warnings |
| 1c-ii | **PASS** | 0 errors, 0 warnings |

---

### fixture-12-1c-combined-violations

**Injected errors:** Combined mutations from fixture-10 (base R4) and fixture-11 (US Core).

**Notes:** Both 1c-i and 1c-ii should fail.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **PASS** | — |
| 1b | **PASS** | — |
| 1c-i | **PASS** | 0 errors, 0 warnings |
| 1c-ii | **PASS** | 0 errors, 0 warnings |

---

### fixture-13-1b-all-structural-errors

**Injected errors:** Patient.ndjson: UTF-8 corruption in 3 lines. Condition.ndjson: 5 lines truncated at 40 chars. Encounter.ndjson: resourceType removed from 5 records. Coverage.ndjson: emptied to 0 bytes.

**Notes:** Multiple 1b failure modes in one extract.

| Stage | Status | Detail |
|-------|--------|--------|
| 1a2 | **FAIL** | failed checks: single_type_per_file |
| 1b | **FAIL** | 3 file(s) failed, 1 empty; Condition.ndjson: all_lines_valid_json; Coverage.ndjson: no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type; Encounter.ndjson: resource_type_present; Patient.ndjson: utf8_decodable, no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type |
| 1c-i | **NOT_RUN** | — |
| 1c-ii | **NOT_RUN** | — |

**Stage 1b file-level failures:**

- `Condition.ndjson` — **FAIL**
  - Failed checks: all_lines_valid_json
  - Issue counts: {'utf8_errors': 0, 'json_parse_errors': 5, 'missing_resource_type': 0, 'resource_type_mismatches': 0}
- `Coverage.ndjson` — **EMPTY**
  - Failed checks: no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type
- `Encounter.ndjson` — **FAIL**
  - Failed checks: resource_type_present
  - Issue counts: {'utf8_errors': 0, 'json_parse_errors': 0, 'missing_resource_type': 5, 'resource_type_mismatches': 0}
- `Patient.ndjson` — **FAIL**
  - Failed checks: utf8_decodable, no_empty_lines_only, all_lines_valid_json, resource_type_present, filename_matches_type
  - Issue counts: {'utf8_errors': 1, 'json_parse_errors': 0, 'missing_resource_type': 0, 'resource_type_mismatches': 0}

---

## False Negatives (Pipeline Missed Injected Errors)

The following injected errors were NOT detected by the pipeline:

- **fixture-05-1b-filename-mismatch** / Stage 1a2: Expected FAIL but got PASS — pipeline did not detect injected error
- **fixture-10-1c-base-r4-errors** / Stage 1c-i: Expected FAIL but got PASS — pipeline did not detect injected error
- **fixture-11-1c-uscore-errors** / Stage 1c-ii: Expected FAIL but got PASS — pipeline did not detect injected error
- **fixture-12-1c-combined-violations** / Stage 1c-i: Expected FAIL but got PASS — pipeline did not detect injected error
- **fixture-12-1c-combined-violations** / Stage 1c-ii: Expected FAIL but got PASS — pipeline did not detect injected error

## Unexpected Results

- **fixture-04-1b-missing-resourcetype** / Stage 1a2: Expected PASS, got FAIL
- **fixture-13-1b-all-structural-errors** / Stage 1a2: Expected PASS, got FAIL

## Recommendations for Code Changes

1. **Stage 1a2 — Filename/content mismatch (fixture-05):** When Procedure.ndjson contains Observation records, 1a2 parses the content and sees Observation resources (not Procedure). This means 1a2 counts Observation but has no Procedure. The `single_type_per_file` check in 1a2 fires only if multiple types appear in ONE file — it does not fire when the filename stem doesn't match the content type. Recommend adding a `filename_matches_content` check to stage1a2 that cross-references filename stems against detected resource types per file.

2. **Stage 1c-i — Missing required R4 fields (fixture-10):** Stripping `status` from Observation and `status`+`intent` from MedicationRequest should produce HAPI R4 errors. If 1c-i passed, investigate whether HAPI CLI is actually validating the resources or whether the JAR version is too lenient. Try running the validator manually on a single stripped Observation record.

3. **Stage 1c-ii — US Core required fields (fixture-11):** Removing Patient.identifier and Condition.category should produce US Core 6.1.0 conformance errors. If 1c-ii passed, verify that the US Core 6.1.0 IG is being correctly loaded by HAPI (`-ig hl7.fhir.us.core#6.1.0`). Also confirm that the test resources have the appropriate US Core profile URLs in `meta.profile` — HAPI may not auto-apply US Core profiles without them.

4. **Stage 1b — Overall PASS/FAIL aggregation:** Currently `stage1b.run()` prints per-file results and returns a report dict, but the script uses `files_failed > 0` to determine overall FAIL. Ensure the pipeline's orchestrator layer `stage1/orchestrate.py` applies the same aggregation logic so the overall pipeline halts correctly on 1b failures before Stage 1c.

5. **Stage 1a2 — Cross-referencing filename vs. content resource type:** The current `single_type_per_file` check catches files with multiple types but not the case where a file has exactly one type that doesn't match the filename. Adding a dedicated `filename_matches_content` check would catch fixture-05-style mislabeling.
